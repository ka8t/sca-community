# @audit-fixture
# Covers: ci_docker_privileged (clean)

stages:
  - build

build:
  stage: build
  image: docker:24-dind
  services:
    - name: docker:24-dind
      options: --cap-add=SYS_ADMIN
  script:
    - docker build -t myapp:latest .
    - docker push myapp:latest
