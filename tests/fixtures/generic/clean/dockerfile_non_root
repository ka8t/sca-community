# @audit-fixture
# @rule: dockerfile_root_user
# @category: security
# @expected: clean
FROM python:3.11-slim
RUN pip install flask
RUN useradd -m appuser
USER appuser
COPY . /app
WORKDIR /app
CMD ["python", "app.py"]
